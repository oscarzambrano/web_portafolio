# SITE SETUP
# Guide: https://docs.hugoblox.com/tutorial/
# Documentation: https://docs.hugoblox.com/
# This file is formatted using YAML syntax - learn more at https://learnxinyminutes.com/docs/yaml/

# ==============================================================================
# HUGO BLOX CONFIGURATION
# ==============================================================================
hugoblox:
  # ────────────────────────────────────────────────────────────────────────────
  # BRANDING
  # Your site's identity - used in navbar, footer, SEO, and copyright
  # ────────────────────────────────────────────────────────────────────────────
  branding:
    # Display name shown in navbar and general site display
    name: "Oscar Zambrano"
    # Optional: Browser tab title (defaults to name if not set)
    site_title: "Oscar Zambrano — Subgerente Data Analytics e IA"
    # Optional: Legal/copyright entity (defaults to name if not set)
    # Entidad de copyright del pie. Es un sitio personal: el titular es
    # Oscar, no su empleador. El empleador se muestra en el perfil,
    # desde content/authors/admin/_index.md.
    organization: "Oscar Zambrano"

  # ────────────────────────────────────────────────────────────────────────────
  # THEME
  # Controls colors and appearance
  # ────────────────────────────────────────────────────────────────────────────
  theme:
    # Mode: 'light', 'dark', or 'system' (follows OS preference)
    mode: system
    # Theme pack: name from data/themes/ or { light: "x", dark: "y" } for mix-and-match
    pack: "default"
    # Optional: Override theme pack colors
    # colors:
    #   primary: "indigo"    # Tailwind palette name or hex (e.g., "#6366f1")
    #   secondary: "emerald"
    #   neutral: "stone"     # Affects gray utilities (text-gray-*, bg-gray-*)

  # ────────────────────────────────────────────────────────────────────────────
  # TYPOGRAPHY
  # Controls fonts and text sizing
  # ────────────────────────────────────────────────────────────────────────────
  typography:
    # Font: 'sans' (Inter), 'serif' (Roboto Slab), or 'native' (system fonts)
    font: "sans"
    # Size: 'sm', 'md', 'lg', or 'xl' (affects base font size and scale)
    size: "md"

  # ────────────────────────────────────────────────────────────────────────────
  # LAYOUT
  # Controls spacing and visual structure
  # ────────────────────────────────────────────────────────────────────────────
  layout:
    # Border radius: 'none', 'sm', 'md', 'lg', or 'full'
    radius: "md"
    # Spacing density: 'compact', 'comfortable', or 'spacious'
    spacing: "comfortable"

# ==============================================================================
# SEO & MARKETING
# ==============================================================================
marketing:
  seo:
    site_type: Person
    local_business_type: ''
    description: 'Oscar Zambrano, Subgerente de Data Analytics e Inteligencia Artificial en Martínez y Valdivieso (NS Agro), Chile. Machine learning y LLMs en producción sobre SAP BTP y SAP HANA Cloud para el agro.'
    twitter: ''
    llm:
      allow:
        - /
      disallow: []
      note: 'Guidance for AI crawlers - customize to match your public content.'
      contact: ''
      policy_url: ''
  analytics:
    google_analytics: ''
    baidu_tongji: ''
    plausible: ''
    fathom: ''
    pirsch: ''
  verification:
    google: ''
    baidu: ''

# ==============================================================================
# SITE HEADER
# ==============================================================================
header:
  navbar:
    enable: true
    block: 'navbar'
    fixed_to_top: true
    # Buscador: un sitio de dos páginas no lo necesita, y evita depender de
    # pagefind en el build.
    show_search: false
    show_mode_toggle: true
    # Selector de paleta: es una función de demo de la plantilla. Además su
    # etiqueta está hardcodeada en inglés en el tema.
    show_theme_picker: false

# ==============================================================================
# SITE FOOTER
# ==============================================================================
footer:
  copyright:
    # Use {year} for current year, {name} for branding.organization, {license} for CC license link
    notice: '© {year} {name}'
    license:
      # Un CV personal no necesita insignia de licencia Creative Commons.
      enable: false

# ==============================================================================
# LOCALIZATION
# ==============================================================================
locale:
  date_format: 'Jan 2, 2006'
  time_format: '3:04 PM'

# ==============================================================================
# SITE FEATURES
# ==============================================================================
features:
  math:
    enable: false
  privacy_pack:
    enable: false
